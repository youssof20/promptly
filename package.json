{
  "name": "promptly",
  "version": "2.0.0",
  "description": "Open source AI prompt optimizer browser extension",
  "private": true,
  "workspaces": [
    "apps/extension"
  ],
  "scripts": {
    "dev": "cd apps/extension && npm run dev",
    "build": "cd apps/extension && npm run build",
    "lint": "cd apps/extension && npm run lint",
    "type-check": "cd apps/extension && npm run type-check"
  },
  "devDependencies": {
    "@types/node": "^20.8.0",
    "typescript": "^5.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "packageManager": "npm@10.0.0"
}
