{
  "name": "promptly-extension",
  "version": "1.0.0",
  "description": "Promptly browser extension for AI prompt optimization",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && npm run postbuild",
    "postbuild": "node scripts/postbuild.js",
    "build:chrome": "npm run build",
    "build:firefox": "npm run build:firefox-manifest && npm run build",
    "build:firefox-manifest": "node scripts/copy-firefox-manifest.js",
    "build:edge": "npm run build",
    "build:all": "npm run build:chrome && npm run build:firefox && npm run build:edge",
    "preview": "vite preview",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.246",
    "@types/node": "^20.8.0",
    "terser": "^5.44.0",
    "typescript": "^5.2.2",
    "vite": "^4.4.9",
    "vite-plugin-static-copy": "^0.13.0"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
